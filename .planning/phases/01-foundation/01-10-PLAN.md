---
phase: 01-foundation
plan: 10
type: execute
wave: 2
depends_on: [01-09]
files_modified:
  - .planning/phases/01-foundation/01-10-SCHEMA-PUSH.md
  - docs/qa/phase1-two-user-shared-stream.md
  - package.json
  - scripts/verify-phase1-supabase-env.mjs
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, HOUSE-01, HOUSE-02, HOUSE-03, HOUSE-04, INFRA-01, PWA-02]
user_setup:
  - service: supabase
    why: "Schema push and live Phase 1 verification require a linked Supabase project."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens, or `supabase login` in a TTY"
      - name: PUBLIC_SUPABASE_URL
        source: "Supabase project API settings"
      - name: PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project API settings"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase project API settings; server/test only, never PUBLIC_"
must_haves:
  truths:
    - "All Phase 1 schema migrations are pushed to the target Supabase project after SQL changes."
    - "A second migration check reports no pending migrations or records explicit evidence if the CLI cannot be authenticated."
    - "Two-user join/shared expense verification path is documented and executable."
    - "Final Phase 1 checks run after schema push, not before."
  artifacts:
    - path: .planning/phases/01-foundation/01-10-SCHEMA-PUSH.md
      provides: "Recorded schema push command output and status"
    - path: scripts/verify-phase1-supabase-env.mjs
      provides: "CLI setup verification for Supabase env values"
    - path: docs/qa/phase1-two-user-shared-stream.md
      provides: "Manual two-user join/shared expense UAT path"
    - path: package.json
      provides: "Verification script entry"
  key_links:
    - from: supabase/migrations/*.sql
      to: target Supabase project
      via: "supabase db push"
      pattern: "supabase db push"
    - from: docs/qa/phase1-two-user-shared-stream.md
      to: HOUSE-04
      via: "two-user shared expense procedure"
      pattern: "User A|User B|shared expense"
---

<objective>
Apply the schema-relevant Phase 1 gap fixes and record final verification.

Purpose: Build/type checks can pass while the target Supabase schema is stale. This plan creates the blocking schema push step required after migration edits and before Phase 1 verification.
Output: Schema push evidence, setup verification script, two-user shared-stream QA path, and post-push test results.
</objective>

<execution_context>
@/Users/ridwan.taufik/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ridwan.taufik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-UAT.md
@.planning/phases/01-foundation/01-08-SUMMARY.md
@.planning/phases/01-foundation/01-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Supabase setup verification command</name>
  <files>scripts/verify-phase1-supabase-env.mjs, package.json</files>
  <read_first>
    - package.json
    - .env.example
    - src/lib/supabase/env.ts
    - .planning/phases/01-foundation/01-UAT.md
  </read_first>
  <behavior>
    - Test 1: running `npm run verify:supabase-env` with missing env exits non-zero and prints `PUBLIC_SUPABASE_URL is missing`.
    - Test 2: running `npm run verify:supabase-env` with `PUBLIC_SUPABASE_URL=https://placeholder.supabase.co` exits non-zero before any fetch.
    - Test 3: running `npm run verify:supabase-env` with real-looking `PUBLIC_SUPABASE_URL`, `PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` prints `Supabase Phase 1 env looks ready.`
  </behavior>
  <action>
    Create `scripts/verify-phase1-supabase-env.mjs`. It must read `PUBLIC_SUPABASE_URL`, `PUBLIC_SUPABASE_ANON_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` from `process.env`. It must reject missing URL, `placeholder.supabase.co`, missing anon key, anon key values `placeholder`, `your-anon-key`, `replace-me`, anon keys shorter than 20 characters, and missing service role key. It must not call `fetch`. On success, print exactly `Supabase Phase 1 env looks ready.` and exit 0. On failure, print the specific failure message and the setup hint `Set PUBLIC_SUPABASE_URL, PUBLIC_SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY with real Supabase project values, then retry.` and exit 1.

    Add package script `"verify:supabase-env": "node scripts/verify-phase1-supabase-env.mjs"` to `package.json`.
  </action>
  <verify>
    <automated>node scripts/verify-phase1-supabase-env.mjs; test "$?" -ne 0</automated>
    <automated>PUBLIC_SUPABASE_URL=https://abcd.supabase.co PUBLIC_SUPABASE_ANON_KEY=abcdefghijklmnopqrstuvwxyz SUPABASE_SERVICE_ROLE_KEY=abcdefghijklmnopqrstuvwxyz npm run verify:supabase-env</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase1-supabase-env.mjs` contains `Supabase Phase 1 env looks ready.`
    - `scripts/verify-phase1-supabase-env.mjs` contains `placeholder.supabase.co`.
    - `scripts/verify-phase1-supabase-env.mjs` does not contain `fetch(`.
    - `package.json` contains `"verify:supabase-env": "node scripts/verify-phase1-supabase-env.mjs"`.
    - The success command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>UAT database setup blocker has a deterministic preflight command before app or migration verification.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push Supabase schema after migration changes</name>
  <files>.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md</files>
  <read_first>
    - supabase/migrations/2026042501_phase1_foundation.sql
    - supabase/migrations/2026042601_invite_lookup_rpc.sql
    - supabase/migrations/2026042602_invite_reuse_rpc.sql
    - .planning/phases/01-foundation/01-09-SUMMARY.md
    - .planning/phases/01-foundation/01-VERIFICATION.md
    - .planning/phases/01-foundation/01-UAT.md
  </read_first>
  <action>
    This task is blocking and must run after Plan 01-09 modifies SQL migrations and before final verification.

    Run `npm run verify:supabase-env` first. If it fails, create `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` with status `BLOCKED`, the failing command, and the exact missing env values. Do not mark this plan complete until the blocker is resolved.

    Run the required Supabase command:

    ```bash
    supabase db push
    ```

    If the Supabase CLI reports an auth/non-TTY error, set `SUPABASE_ACCESS_TOKEN` in the environment and rerun `supabase db push`. If the CLI prompts for confirmation and the executor cannot answer non-interactively, record `BLOCKED: supabase db push requires interactive confirmation` in `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` and stop.

    After a successful push, run `supabase db push` a second time to prove the target schema is current. Record both command invocations, timestamps, exit statuses, and the final no-pending-migrations output in `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation/01-10-SCHEMA-PUSH.md && rg -n "supabase db push|status: (APPLIED|CURRENT)|second push|no pending|No migrations" .planning/phases/01-foundation/01-10-SCHEMA-PUSH.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` exists.
    - `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` contains `supabase db push`.
    - `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` contains either `status: APPLIED` or `status: CURRENT`.
    - `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` contains `second push`.
    - This task is not complete if `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` contains `status: BLOCKED`.
  </acceptance_criteria>
  <done>The target Supabase schema is applied/current after Phase 1 SQL fixes, with evidence recorded before final verification.</done>
</task>

<task type="auto">
  <name>Task 3: Document and run post-push Phase 1 verification path</name>
  <files>docs/qa/phase1-two-user-shared-stream.md, .planning/phases/01-foundation/01-10-SCHEMA-PUSH.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-10-SCHEMA-PUSH.md
    - tests/db/phase1-rls.test.ts
    - src/routes/(app)/+page.server.ts
    - src/routes/(app)/onboarding/join/+page.server.ts
    - src/routes/(app)/settings/invite/+page.server.ts
    - .planning/phases/01-foundation/01-VERIFICATION.md
    - .planning/phases/01-foundation/01-UAT.md
  </read_first>
  <action>
    Create `docs/qa/phase1-two-user-shared-stream.md` with a concrete two-user verification path:
    1. User A signs up or logs in at `/auth`.
    2. User A creates household `Phase 1 QA Household`.
    3. User A opens `/settings/invite`, copies the active invite code, refreshes the page, and confirms the code is unchanged while active.
    4. User B signs up or logs in in a separate browser profile/session.
    5. User B opens `/onboarding/join`, enters the code, sees confirmation household name `Phase 1 QA Household`, confirms join, and lands in the signed-in shell.
    6. Insert one shared test expense for User A's household using SQL in Supabase SQL editor or psql:
       `insert into public.expenses (household_id, created_by, amount, category, note, spent_at, client_id) values ('<household_id>', '<user_a_id>', 45000, 'Food', 'Phase 1 shared stream QA', now(), gen_random_uuid());`
    7. Refresh User A and User B signed-in shells; both must show `Food`, `Rp 45k`, and `Phase 1 shared stream QA`.
    8. Create or use a third signed-in user outside the household; querying the same household expenses through anon-authenticated client must return `[]`.

    After the schema push evidence is `APPLIED` or `CURRENT`, run:
    - `npm run verify:supabase-env`
    - `npm run test:unit -- phase1-rls`
    - `npm run test:unit -- onboarding invite-rpcs shared-shell auth-session env`
    - `npm run check`
    - `npm run build`

    Append a `Post-push verification` section to `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` with each command and pass/fail status.
  </action>
  <verify>
    <automated>npm run verify:supabase-env</automated>
    <automated>npm run test:unit -- phase1-rls</automated>
    <automated>npm run test:unit -- onboarding invite-rpcs shared-shell auth-session env</automated>
    <automated>npm run check</automated>
    <automated>npm run build</automated>
    <automated>rg -n "User A|User B|Phase 1 QA Household|Phase 1 shared stream QA|Food|Rp 45k|\\[\\]" docs/qa/phase1-two-user-shared-stream.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/qa/phase1-two-user-shared-stream.md` contains `User A`, `User B`, `Phase 1 QA Household`, `Phase 1 shared stream QA`, and `Rp 45k`.
    - `docs/qa/phase1-two-user-shared-stream.md` contains the SQL fragment `insert into public.expenses`.
    - `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` contains `Post-push verification`.
    - `.planning/phases/01-foundation/01-10-SCHEMA-PUSH.md` contains `npm run test:unit -- phase1-rls`.
    - All automated commands in `<verify>` exit 0 after real Supabase env is configured and schema push is applied/current.
  </acceptance_criteria>
  <done>HOUSE-04 has a documented two-user join/shared expense verification path and final checks run after schema apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local env -> Supabase CLI | Local secrets authorize schema changes against the target project. |
| migration files -> remote database | SQL migrations alter RLS and RPC behavior for real users. |
| manual QA sessions -> production-like data | Two browser sessions prove household-scoped sharing without bypassing auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-01 | Tampering | `supabase db push` | mitigate | Require explicit schema push evidence after SQL changes and a second push/current check. |
| T-01-10-02 | Information Disclosure | env verification script | mitigate | Script validates presence and shape only; it must not print secret values. |
| T-01-10-03 | Repudiation | schema apply evidence | mitigate | Record command, timestamp, exit status, and final schema-current output in a phase artifact. |
| T-01-10-04 | Spoofing | two-user QA | mitigate | Use separate authenticated sessions for User A and User B; outsider verification must return `[]`. |
</threat_model>

<verification>
This plan cannot pass until Task 2 records schema status `APPLIED` or `CURRENT`. Then run the full post-push verification command list in Task 3.
</verification>

<success_criteria>
This plan is complete when Phase 1 migrations are applied/current in Supabase, schema evidence is recorded, and the two-user shared-stream path is documented with post-push automated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-10-SUMMARY.md`.
</output>
