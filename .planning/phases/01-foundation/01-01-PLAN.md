---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - svelte.config.js
  - vite.config.ts
  - tsconfig.json
  - src/app.html
  - src/app.d.ts
  - src/app.css
  - .gitignore
  - .env.example
  - vitest.config.ts
  - tests/setup.ts
  - tests/smoke/app-shell.test.ts
autonomous: true
requirements:
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - "The repo becomes a runnable SvelteKit + Svelte 5 + Tailwind 4 application"
    - "Test and typecheck commands exist before feature work starts"
    - "Environment variable scaffolding prevents privileged keys from being read in the client"
  artifacts:
    - path: "package.json"
      provides: "SvelteKit project scripts and dependencies"
      contains: "@sveltejs/kit"
    - path: "src/app.css"
      provides: "Tailwind 4 baseline and app-wide design tokens"
      contains: "@import \"tailwindcss\""
    - path: ".env.example"
      provides: "Documented public env keys for Supabase browser usage"
      contains: "PUBLIC_SUPABASE_URL="
    - path: "vitest.config.ts"
      provides: "Phase 1 test runner configuration"
      contains: "vitest"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test scripts reference the configured runner"
      pattern: "test:unit"
    - from: ".env.example"
      to: "package.json"
      via: "runtime expects public Supabase keys only in the browser"
      pattern: "PUBLIC_SUPABASE"
---

<objective>
Create the greenfield SvelteKit project scaffold, install the locked Phase 1 stack, add Tailwind 4 and baseline test tooling, and establish environment-file conventions before any auth or database work begins.

Purpose: Every later plan depends on a real application skeleton, repeatable commands, and safe env handling.
Output: A buildable SvelteKit repo with Tailwind, Vitest, and documented public env keys.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<threat_model>
- Threat: privileged Supabase keys leak into the browser bundle through sloppy env handling.
  Mitigation: restrict browser-readable vars to `PUBLIC_SUPABASE_URL` and `PUBLIC_SUPABASE_ANON_KEY`; document any future server-only keys separately.
- Threat: feature work starts without repeatable verification and silently regresses fundamentals.
  Mitigation: install test/typecheck scripts and make them part of this plan's acceptance criteria.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the SvelteKit/Tailwind foundation in the existing repo</name>
  <files>package.json, svelte.config.js, vite.config.ts, tsconfig.json, src/app.html, src/app.d.ts, src/app.css, .gitignore</files>
  <read_first>.planning/STATE.md, .planning/PROJECT.md, .planning/phases/01-foundation/01-RESEARCH.md, package.json</read_first>
  <action>
Create the initial SvelteKit 2 TypeScript application in the current repository root using the locked stack from `.planning/STATE.md`. Ensure `package.json` includes `svelte`, `@sveltejs/kit`, `vite`, `typescript`, `@tailwindcss/vite`, `tailwindcss`, `vitest`, and `@testing-library/svelte`. Configure `vite.config.ts` to load the SvelteKit plugin and Tailwind 4 via `tailwindcss()` from `@tailwindcss/vite`. Set `src/app.css` to import Tailwind using `@import "tailwindcss";`, then define CSS custom properties reflecting the UI-SPEC palette: background `#F7F3EB`, surface `#E7DED0`, accent `#176B5D`, destructive `#B9382F`. Keep the initial route shell minimal; do not add auth logic in this plan. Update `.gitignore` to include `node_modules`, `.svelte-kit`, `build`, `.vercel`, `.env`, and local Supabase artifacts if generated later.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - package.json contains `"@sveltejs/kit"`
    - package.json contains `"@tailwindcss/vite"`
    - src/app.css contains `@import "tailwindcss";`
    - src/app.css contains `--color-accent: #176B5D` or equivalent CSS token using `#176B5D`
    - .gitignore contains `.svelte-kit`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The repo builds as a SvelteKit application with Tailwind 4 wired and no auth-specific behavior yet.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit env and script conventions for safe Supabase usage</name>
  <files>package.json, .env.example</files>
  <read_first>package.json, .planning/phases/01-foundation/01-RESEARCH.md, .planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>
Update `package.json` scripts to include `dev`, `build`, `preview`, `check`, `test`, and `test:unit`, with `test` and `test:unit` both invoking Vitest in non-watch mode. Create `.env.example` containing exactly `PUBLIC_SUPABASE_URL=`, `PUBLIC_SUPABASE_ANON_KEY=`, and `SUPABASE_PROJECT_REF=`. Do not add `SUPABASE_SERVICE_ROLE_KEY` to `.env.example` because the research explicitly forbids client-side privileged access. Add a short comment in `.env.example` stating that service-role keys must stay server-only and out of the browser bundle.
  </action>
  <verify><automated>rg -n "PUBLIC_SUPABASE_URL|PUBLIC_SUPABASE_ANON_KEY|test:unit" package.json .env.example</automated></verify>
  <acceptance_criteria>
    - package.json contains `"test:unit"`
    - package.json contains `"check"`
    - .env.example contains `PUBLIC_SUPABASE_URL=`
    - .env.example contains `PUBLIC_SUPABASE_ANON_KEY=`
    - .env.example does not contain `SERVICE_ROLE`
  </acceptance_criteria>
  <done>Scripts and env scaffolding are in place and explicitly protect against privileged-key misuse.</done>
</task>

<task type="auto">
  <name>Task 3: Establish Wave 0 test infrastructure</name>
  <files>vitest.config.ts, tests/setup.ts, tests/smoke/app-shell.test.ts, package.json</files>
  <read_first>package.json, .planning/phases/01-foundation/01-VALIDATION.md, vitest.config.ts</read_first>
  <action>
Create `vitest.config.ts` configured for the SvelteKit app, jsdom environment, and `tests/setup.ts` as the setup entry. In `tests/setup.ts`, install Testing Library matchers and mock browser APIs that Phase 1 will later use, including `window.matchMedia`, `window.localStorage`, and a placeholder `navigator.standalone` getter if the runtime lacks one. Add `tests/smoke/app-shell.test.ts` with a baseline render test that imports a trivial Svelte component or route shell and verifies the app can render under Vitest. Keep the test narrow: the purpose is to prove the toolchain works before later plans add behavior-heavy tests.
  </action>
  <verify><automated>npm run test:unit</automated></verify>
  <acceptance_criteria>
    - vitest.config.ts contains `environment: 'jsdom'` or `environment: "jsdom"`
    - vitest.config.ts contains `setupFiles`
    - tests/setup.ts contains `matchMedia`
    - tests/smoke/app-shell.test.ts contains `render(`
    - `npm run test:unit` exits 0
  </acceptance_criteria>
  <done>Wave 0 verification exists and later plans can rely on `npm run test:unit` from the start.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds
- [ ] `npm run check` passes
- [ ] `npm run test:unit` passes
- [ ] `.env.example` exposes only public Supabase browser keys
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No privileged Supabase env key is documented for client use
- The repo now contains a working SvelteKit + Tailwind + Vitest baseline for later Phase 1 work

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
