---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - src/lib/components/InstallGuidanceBanner.svelte
  - src/lib/install/visibility.ts
  - src/routes/(app)/+layout.svelte
  - src/routes/(app)/+page.server.ts
  - src/routes/(app)/+page.svelte
  - src/routes/(app)/household/+page.server.ts
  - src/routes/(app)/household/+page.svelte
  - tests/households/shared-shell.test.ts
autonomous: true
requirements:
  - HOUSE-04
  - PWA-02
user_setup: []
must_haves:
  truths:
    - "After create or join completes, users land on a household-connected shell rather than a placeholder dashboard"
    - "The easiest reachable action from the shell is viewing household details"
    - "Install guidance appears only after auth in Safari and never in standalone mode"
    - "The app has a narrow household-scoped shared-data proof that both members can read"
  artifacts:
    - path: "src/routes/(app)/+page.svelte"
      provides: "Signed-in household status shell"
      contains: "View household details"
    - path: "src/lib/components/InstallGuidanceBanner.svelte"
      provides: "Safari install guidance UI"
      contains: "Tap Share, then Add to Home Screen"
    - path: "src/routes/(app)/household/+page.svelte"
      provides: "Household identity and member summary surface"
      contains: "Members"
    - path: "tests/households/shared-shell.test.ts"
      provides: "Regression coverage for install-banner visibility and shared shell data"
      contains: "standalone"
  key_links:
    - from: "src/lib/install/visibility.ts"
      to: "src/lib/components/InstallGuidanceBanner.svelte"
      via: "banner renders only when the visibility helper returns true"
      pattern: "shouldShowInstallGuidance"
---

<objective>
Build the signed-in household shell, household details screen, Safari install-guidance banner, and a narrow shared-data proof for the household scope.

Purpose: Phase 1 should end with a real household-connected experience, not just raw onboarding screens or a placeholder success message.
Output: Main signed-in shell, household details route, install-guidance component/logic, and shared-shell tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-04-PLAN.md
</context>

<threat_model>
- Threat: install guidance appears in the wrong context, creating a broken or spammy UX.
  Mitigation: centralize visibility rules in a helper that gates on auth success, Safari context, standalone state, and snooze state.
- Threat: HOUSE-04 is treated as satisfied without any shared household-scoped read proof.
  Mitigation: add a read-only shared-data section and tests proving shell data is household-scoped.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build the signed-in shell and household details surfaces</name>
  <files>src/routes/(app)/+layout.svelte, src/routes/(app)/+page.server.ts, src/routes/(app)/+page.svelte, src/routes/(app)/household/+page.server.ts, src/routes/(app)/household/+page.svelte</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-UI-SPEC.md, src/routes/(app)/+page.svelte, src/routes/(app)/household/+page.svelte</read_first>
  <action>
Create the main signed-in shell as a household-connected status surface, not a dashboard for future features. Load the current household, member list, and a narrow read-only shared-data proof in `+page.server.ts`; the proof can be a recent-expense summary or a small shared activity list sourced from the household-scoped `expenses` table created in Plan 02. In `src/routes/(app)/+page.svelte`, render household identity, member status, and a primary action labeled exactly `View household details`. In `src/routes/(app)/household/+page.svelte`, render one summary view that combines household identity and member status in the same surface.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - src/routes/(app)/+page.svelte contains `View household details`
    - src/routes/(app)/+page.server.ts reads household-scoped data
    - src/routes/(app)/household/+page.svelte contains `Members`
    - src/routes/(app)/household/+page.svelte contains household name rendering
  </acceptance_criteria>
  <done>Signed-in users land on a real household shell with an obvious path to details and a household-scoped data proof.</done>
</task>

<task type="auto">
  <name>Task 2: Add Safari install guidance with snooze and standalone protection</name>
  <files>src/lib/install/visibility.ts, src/lib/components/InstallGuidanceBanner.svelte, src/routes/(app)/+page.svelte</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-RESEARCH.md, .planning/phases/01-foundation/01-UI-SPEC.md, src/lib/install/visibility.ts</read_first>
  <action>
Create `src/lib/install/visibility.ts` exporting a helper such as `shouldShowInstallGuidance(...)` that returns true only when the user is authenticated, the browser is Safari/iOS installable context, the app is not running standalone, and the local snooze window has expired. Create `src/lib/components/InstallGuidanceBanner.svelte` as a non-modal banner/card with the exact instructional copy `Tap Share, then Add to Home Screen`. Wire the banner into the signed-in shell after auth has succeeded; do not show it on first paint or on unauthenticated routes. Snoozing should store a timestamp or flag in lightweight client storage so the banner can reappear later rather than disappear forever.
  </action>
  <verify><automated>npm run test:unit -- shared-shell</automated></verify>
  <acceptance_criteria>
    - src/lib/components/InstallGuidanceBanner.svelte contains `Tap Share, then Add to Home Screen`
    - src/lib/install/visibility.ts contains `standalone`
    - src/lib/install/visibility.ts contains `Safari` or an explicit Safari detection branch
    - src/routes/(app)/+page.svelte renders `InstallGuidanceBanner`
  </acceptance_criteria>
  <done>Install guidance appears only in the right context and follows the locked Phase 1 UX rules.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared-shell regression coverage</name>
  <files>tests/households/shared-shell.test.ts</files>
  <read_first>.planning/phases/01-foundation/01-VALIDATION.md, src/lib/install/visibility.ts, src/routes/(app)/+page.server.ts, tests/households/shared-shell.test.ts</read_first>
  <action>
Create `tests/households/shared-shell.test.ts` covering these cases: install guidance is hidden when standalone is true, install guidance is hidden on unauthenticated state, install guidance is shown after auth in Safari context, and the signed-in shell loader returns household-scoped shared data for joined members. The shared-data assertion should prove the shell reads household-scoped data rather than only user-local profile data.
  </action>
  <verify><automated>npm run test:unit -- shared-shell</automated></verify>
  <acceptance_criteria>
    - tests/households/shared-shell.test.ts contains `standalone`
    - tests/households/shared-shell.test.ts contains `Tap Share, then Add to Home Screen`
    - tests/households/shared-shell.test.ts contains `household`
    - `npm run test:unit -- shared-shell` exits 0
  </acceptance_criteria>
  <done>The shell and install-guidance behavior are protected by regression tests, including household-scoped read proof.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check` passes
- [ ] `npm run test:unit -- shared-shell` passes
- [ ] Signed-in shell leads with household-connected status, not future-feature previews
- [ ] Install guidance only appears in post-auth Safari context and not in standalone mode
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Users who complete create/join land on a meaningful connected shell
- Phase 1 includes a concrete household-scoped shared-data proof and Safari install guidance

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
