---
phase: 01-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/supabase-keepalive.yml
  - scripts/supabase-keepalive.mjs
  - docs/ops/supabase-keepalive.md
autonomous: true
requirements:
  - INFRA-01
user_setup:
  - "Add the keep-alive target URL and any required secrets in the GitHub repository settings before enabling the workflow in production."
must_haves:
  truths:
    - "A GitHub Actions cron exists to prevent Supabase free-tier inactivity pause"
    - "The keep-alive workflow uses repository secrets or env vars, not hard-coded project credentials"
    - "There is local documentation describing why the workflow exists"
  artifacts:
    - path: ".github/workflows/supabase-keepalive.yml"
      provides: "Scheduled keep-alive automation"
      contains: "schedule:"
    - path: "scripts/supabase-keepalive.mjs"
      provides: "Minimal keep-alive request script"
      contains: "fetch("
    - path: "docs/ops/supabase-keepalive.md"
      provides: "Operational documentation for setup and troubleshooting"
      contains: "Supabase"
  key_links:
    - from: ".github/workflows/supabase-keepalive.yml"
      to: "scripts/supabase-keepalive.mjs"
      via: "workflow invokes the script on the schedule"
      pattern: "node scripts/supabase-keepalive.mjs"
---

<objective>
Add the Supabase keep-alive automation and operational docs required to keep the free-tier project from pausing.

Purpose: Phase 1 explicitly includes infra hygiene, and this is easiest to forget if it is not codified as a plan with its own verification.
Output: Scheduled GitHub Actions workflow, small keep-alive script, and setup/troubleshooting documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<threat_model>
- Threat: hard-coded project URLs or privileged keys leak through CI configuration.
  Mitigation: read target URL and auth material from GitHub secrets or env, never inline credentials.
- Threat: future maintainers remove the workflow because they do not know why it exists.
  Mitigation: document the free-tier pause risk and expected setup in repo docs.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the keep-alive workflow and request script</name>
  <files>.github/workflows/supabase-keepalive.yml, scripts/supabase-keepalive.mjs</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md, .github/workflows/supabase-keepalive.yml, scripts/supabase-keepalive.mjs</read_first>
  <action>
Create `.github/workflows/supabase-keepalive.yml` scheduled at least every 5 days, but preferably twice weekly for margin. The workflow should check out the repo, install Node only if needed, and run `node scripts/supabase-keepalive.mjs`. In `scripts/supabase-keepalive.mjs`, read a keep-alive URL from an env var such as `SUPABASE_KEEPALIVE_URL`, perform a simple `fetch()` request, fail on non-2xx responses, and print a short success/failure message. Do not hard-code any project URL, token, or SQL credentials into the workflow or script.
  </action>
  <verify><automated>rg -n "schedule:|SUPABASE_KEEPALIVE_URL|fetch\\(" .github/workflows/supabase-keepalive.yml scripts/supabase-keepalive.mjs</automated></verify>
  <acceptance_criteria>
    - .github/workflows/supabase-keepalive.yml contains `schedule:`
    - .github/workflows/supabase-keepalive.yml contains `node scripts/supabase-keepalive.mjs`
    - scripts/supabase-keepalive.mjs contains `SUPABASE_KEEPALIVE_URL`
    - scripts/supabase-keepalive.mjs contains `fetch(`
  </acceptance_criteria>
  <done>The repo contains working keep-alive automation without embedded secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Document setup, secrets, and failure handling for the keep-alive job</name>
  <files>docs/ops/supabase-keepalive.md</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md, docs/ops/supabase-keepalive.md</read_first>
  <action>
Create `docs/ops/supabase-keepalive.md` describing why Supabase free-tier inactivity pause matters for xtrack, which GitHub repository secrets or variables must be set, how often the cron runs, and what to do if the job starts failing. Include the exact secret name `SUPABASE_KEEPALIVE_URL` and mention that credentials must not be committed. Keep the doc short and operational.
  </action>
  <verify><automated>rg -n "SUPABASE_KEEPALIVE_URL|free-tier|GitHub Actions" docs/ops/supabase-keepalive.md</automated></verify>
  <acceptance_criteria>
    - docs/ops/supabase-keepalive.md contains `SUPABASE_KEEPALIVE_URL`
    - docs/ops/supabase-keepalive.md contains `free-tier`
    - docs/ops/supabase-keepalive.md contains `GitHub Actions`
  </acceptance_criteria>
  <done>The keep-alive automation is documented well enough that it can be maintained later.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Workflow schedule exists
- [ ] Script reads target URL from env, not hard-coded strings
- [ ] Ops doc explains setup and troubleshooting
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- INFRA-01 is represented by durable automation and documentation
- No Supabase secrets are committed into source control

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
