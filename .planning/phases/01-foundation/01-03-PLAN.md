---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/auth/schemas.ts
  - src/lib/auth/session.ts
  - src/hooks.server.ts
  - src/routes/(auth)/+page.svelte
  - src/routes/(auth)/+page.server.ts
  - src/routes/logout/+server.ts
  - src/routes/(app)/+layout.server.ts
  - tests/auth/auth-session.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - "Users can sign up and log in with email and password from one combined auth surface"
    - "Protected app routes redirect unauthenticated users to the auth route"
    - "Logout ends the session cleanly"
    - "Standalone-mode app boot attempts session restoration before rendering a broken signed-in shell"
  artifacts:
    - path: "src/routes/(auth)/+page.svelte"
      provides: "Combined login/sign-up UI"
      contains: "Log in"
    - path: "src/hooks.server.ts"
      provides: "Request-scoped session hydration and auth guard behavior"
      contains: "event.locals"
    - path: "src/routes/logout/+server.ts"
      provides: "Explicit logout endpoint"
      contains: "signOut"
    - path: "tests/auth/auth-session.test.ts"
      provides: "Session restoration and auth route regression coverage"
      contains: "standalone"
  key_links:
    - from: "src/lib/supabase/server.ts"
      to: "src/hooks.server.ts"
      via: "server-side auth client creation"
      pattern: "createServer"
    - from: "src/routes/(auth)/+page.server.ts"
      to: "src/lib/auth/schemas.ts"
      via: "form validation"
      pattern: "email"
---

<objective>
Implement the email/password auth flow, request-scoped session handling, protected app routing, logout, and standalone-first session restoration behavior.

Purpose: Phase 1 is not successful unless auth works in the installed-PWA context and users are never stranded after install.
Output: Combined auth route, Supabase auth helpers, guarded app layout, logout endpoint, and auth/session tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/01-foundation/01-02-PLAN.md
</context>

<threat_model>
- Threat: unauthenticated users can reach household routes because request guards are incomplete.
  Mitigation: centralize session hydration in `src/hooks.server.ts` and gate `(app)` routes in layout logic.
- Threat: Safari-to-standalone context switch leaves users on a broken authenticated shell with no usable session.
  Mitigation: add explicit restoration logic before protected shell render and test the standalone branch.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase auth clients and request-scoped session plumbing</name>
  <files>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/auth/session.ts, src/hooks.server.ts, src/routes/(app)/+layout.server.ts</files>
  <read_first>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/hooks.server.ts, .planning/phases/01-foundation/01-RESEARCH.md, .planning/phases/01-foundation/01-CONTEXT.md</read_first>
  <action>
Create browser and server Supabase helpers that read only the public browser keys and attach a typed auth/session object to `event.locals`. In `src/hooks.server.ts`, load the current user on each request, expose it through `event.locals`, and redirect unauthenticated requests for `(app)` routes to the combined auth route. In `src/lib/auth/session.ts`, implement a small standalone-aware helper that checks whether the app is in standalone mode and whether a session restore or refresh attempt must happen before the protected shell renders. Keep this logic explicit and testable; do not hide it in component-only side effects.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - src/hooks.server.ts contains `event.locals`
    - src/routes/(app)/+layout.server.ts contains `redirect`
    - src/lib/auth/session.ts contains `standalone` or `navigator.standalone`
    - src/lib/supabase/client.ts contains `PUBLIC_SUPABASE_URL`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Server requests know the current auth state and protected routes no longer rely on ad hoc component guards.</done>
</task>

<task type="auto">
  <name>Task 2: Build the combined Log in / Sign up screen and logout endpoint</name>
  <files>src/lib/auth/schemas.ts, src/routes/(auth)/+page.svelte, src/routes/(auth)/+page.server.ts, src/routes/logout/+server.ts</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-UI-SPEC.md, src/routes/(auth)/+page.svelte, src/routes/(auth)/+page.server.ts</read_first>
  <action>
Create a single auth route with `Log in` selected by default and a tab or segmented control that switches between login and sign-up. Validate email and password inputs in `src/lib/auth/schemas.ts` and in the corresponding server action. Keep the copy practical and onboarding-oriented, show inline errors on failure, and keep password fields hidden by default with a show/hide toggle. After successful sign-up, immediately create the session and route the user into household onboarding instead of sending them to a separate confirmation step. Create `src/routes/logout/+server.ts` that signs out and redirects back to the auth route.
  </action>
  <verify><automated>npm run test:unit -- auth-session</automated></verify>
  <acceptance_criteria>
    - src/routes/(auth)/+page.svelte contains `Log in`
    - src/routes/(auth)/+page.svelte contains `Sign up`
    - src/routes/(auth)/+page.svelte contains `show` or `Show password`
    - src/routes/(auth)/+page.server.ts contains `signInWithPassword`
    - src/routes/(auth)/+page.server.ts contains `signUp`
    - src/routes/logout/+server.ts contains `signOut`
  </acceptance_criteria>
  <done>Users can authenticate from one screen and exit the session cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Add auth and session regression coverage</name>
  <files>tests/auth/auth-session.test.ts</files>
  <read_first>.planning/phases/01-foundation/01-VALIDATION.md, src/hooks.server.ts, src/lib/auth/session.ts, tests/auth/auth-session.test.ts</read_first>
  <action>
Create `tests/auth/auth-session.test.ts` covering these cases: unauthenticated `(app)` request redirects to auth, login and sign-up server actions call the correct Supabase methods, logout clears the session path, and the standalone-session helper chooses the restoration branch when `navigator.standalone === true`. Prefer stable mocks over network calls. Include at least one assertion that a failed restore path does not silently treat the user as authenticated.
  </action>
  <verify><automated>npm run test:unit -- auth-session</automated></verify>
  <acceptance_criteria>
    - tests/auth/auth-session.test.ts contains `redirect`
    - tests/auth/auth-session.test.ts contains `standalone`
    - tests/auth/auth-session.test.ts contains `signInWithPassword`
    - `npm run test:unit -- auth-session` exits 0
  </acceptance_criteria>
  <done>Auth behavior is guarded by regression tests instead of manual spot checks alone.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check` passes
- [ ] `npm run test:unit -- auth-session` passes
- [ ] Auth route defaults to Log in and supports Sign up
- [ ] Protected routes redirect when session is absent
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Auth, logout, and standalone session restoration are implemented as explicit, testable flows
- No OAuth or magic-link redirect flow is introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
