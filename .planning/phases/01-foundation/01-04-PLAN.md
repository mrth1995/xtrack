---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - src/lib/households/schemas.ts
  - src/lib/server/households/service.ts
  - src/routes/(app)/onboarding/+page.server.ts
  - src/routes/(app)/onboarding/+page.svelte
  - src/routes/(app)/onboarding/create/+page.svelte
  - src/routes/(app)/onboarding/create/+page.server.ts
  - src/routes/(app)/onboarding/join/+page.server.ts
  - src/routes/(app)/onboarding/join/+page.svelte
  - src/routes/(app)/settings/invite/+page.server.ts
  - src/routes/(app)/settings/invite/+page.svelte
  - tests/households/onboarding.test.ts
autonomous: true
requirements:
  - HOUSE-01
  - HOUSE-02
  - HOUSE-03
user_setup: []
must_haves:
  truths:
    - "Authenticated users without a household land on an explicit create-or-join choice screen"
    - "Create household asks only for household name"
    - "Join by code validates on submit, shows confirmation, and handles invalid or expired codes inline"
    - "Invite management lives under settings/menu, not the main shell"
  artifacts:
    - path: "src/routes/(app)/onboarding/+page.svelte"
      provides: "Equal-weight create/join choice screen"
      contains: "Create household"
    - path: "src/routes/(app)/onboarding/join/+page.svelte"
      provides: "Single-purpose join flow"
      contains: "Join with code"
    - path: "src/routes/(app)/settings/invite/+page.svelte"
      provides: "Invite management screen"
      contains: "Copy code"
    - path: "tests/households/onboarding.test.ts"
      provides: "Household flow happy/error coverage"
      contains: "expired"
  key_links:
    - from: "src/lib/server/households/service.ts"
      to: "supabase/migrations/2026042501_phase1_foundation.sql"
      via: "service calls the SQL helpers created in Plan 02"
      pattern: "accept_household_invite"
---

<objective>
Implement the household onboarding and invite-management flows that sit immediately after auth: create household, join via code, confirmation, and settings-based invite access.

Purpose: The Phase 1 product promise is not just account access; it is a secure shared-household setup that leads users into the connected app.
Output: Household onboarding routes, household service helpers, invite management screen, and regression coverage for happy/error paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-02-PLAN.md
@.planning/phases/01-foundation/01-03-PLAN.md
</context>

<threat_model>
- Threat: join flow reveals household data too early or accepts codes via a racy client-only flow.
  Mitigation: validate and join through server-side service methods backed by SQL helpers from Plan 02.
- Threat: invite generation or replacement creates multiple active codes and inconsistent state.
  Mitigation: show current active code if valid and auto-generate only when no active usable code exists.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement server-side household service methods and form validation</name>
  <files>src/lib/households/schemas.ts, src/lib/server/households/service.ts, src/routes/(app)/onboarding/create/+page.server.ts, src/routes/(app)/onboarding/join/+page.server.ts</files>
  <read_first>src/lib/server/households/service.ts, .planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-RESEARCH.md, supabase/migrations/2026042501_phase1_foundation.sql</read_first>
  <action>
Create typed Zod schemas for household creation and invite-code submission in `src/lib/households/schemas.ts`. In `src/lib/server/households/service.ts`, add explicit methods for `createHousehold`, `lookupInviteCode`, `acceptInviteCode`, and `getOrCreateActiveInviteCode`. Use the SQL helpers from Plan 02 for atomic create/join behavior rather than composing multi-step client writes. In the onboarding server actions, ensure create-household requests only ask for `name`, and join-code requests validate only on submit, not on every keystroke.
  </action>
  <verify><automated>npm run test:unit -- onboarding</automated></verify>
  <acceptance_criteria>
    - src/lib/households/schemas.ts contains `name`
    - src/lib/households/schemas.ts contains `code`
    - src/lib/server/households/service.ts contains `acceptHouseholdInvite` or `acceptInviteCode`
    - src/routes/(app)/onboarding/create/+page.server.ts contains `createHousehold`
    - src/routes/(app)/onboarding/join/+page.server.ts contains `lookupInvite`
  </acceptance_criteria>
  <done>Household create/join behavior is implemented on the server side with explicit validation and atomic invite handling.</done>
</task>

<task type="auto">
  <name>Task 2: Build onboarding and invite-management screens that match the locked Phase 1 flow</name>
  <files>src/routes/(app)/onboarding/+page.server.ts, src/routes/(app)/onboarding/+page.svelte, src/routes/(app)/onboarding/create/+page.svelte, src/routes/(app)/onboarding/join/+page.svelte, src/routes/(app)/settings/invite/+page.server.ts, src/routes/(app)/settings/invite/+page.svelte</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md, .planning/phases/01-foundation/01-UI-SPEC.md, src/routes/(app)/onboarding/+page.svelte, src/routes/(app)/onboarding/create/+page.svelte, src/routes/(app)/settings/invite/+page.svelte</read_first>
  <action>
Create the onboarding choice screen with equal-weight `Create household` and `Join with code` actions. Add `src/routes/(app)/onboarding/create/+page.svelte` as the dedicated minimal create-household form that asks only for household name and nothing else. Implement the join route as a single-purpose code-entry page with one standard text input, submit-time validation, inline error reuse, and a secondary switch-to-create action. After a valid lookup, show the household name confirmation state before the final join action. Build the settings-based invite-management route so it shows the active invite code if valid, generates a new one only when the prior code is expired or used, exposes `Copy code` as the primary action, and shows joined status after use. Keep this route out of the main shell.
  </action>
  <verify><automated>npm run check</automated></verify>
  <acceptance_criteria>
    - src/routes/(app)/onboarding/+page.svelte contains `Create household`
    - src/routes/(app)/onboarding/create/+page.svelte contains `name`
    - src/routes/(app)/onboarding/+page.svelte contains `Join with code`
    - src/routes/(app)/onboarding/join/+page.svelte contains `Confirm`
    - src/routes/(app)/settings/invite/+page.svelte contains `Copy code`
    - src/routes/(app)/settings/invite/+page.svelte contains `expires`
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The onboarding UX now matches the locked Phase 1 decisions instead of leaving create/join behavior ambiguous.</done>
</task>

<task type="auto">
  <name>Task 3: Add household onboarding regression coverage</name>
  <files>tests/households/onboarding.test.ts</files>
  <read_first>.planning/phases/01-foundation/01-VALIDATION.md, src/lib/server/households/service.ts, src/routes/(app)/onboarding/join/+page.server.ts, tests/households/onboarding.test.ts</read_first>
  <action>
Create `tests/households/onboarding.test.ts` for these cases: a user without membership lands on onboarding, create-household submits only a name, invalid or expired invite codes stay on the same screen with an inline error, valid invite lookup shows a confirmation step with the household name, and invite-management reuses an existing active code instead of replacing it. Mock service calls and server actions rather than depending on live Supabase.
  </action>
  <verify><automated>npm run test:unit -- onboarding</automated></verify>
  <acceptance_criteria>
    - tests/households/onboarding.test.ts contains `expired`
    - tests/households/onboarding.test.ts contains `Create household`
    - tests/households/onboarding.test.ts contains `Join with code`
    - tests/households/onboarding.test.ts contains `Copy code`
  </acceptance_criteria>
  <done>The onboarding and invite-management flows are covered by regression tests for both happy and error paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run check` passes
- [ ] `npm run test:unit -- onboarding` passes
- [ ] Join flow shows confirmation before final join
- [ ] Invite management is reachable from settings/menu, not the main shell
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Household creation, invite generation, and invite acceptance work with the exact flow locked in CONTEXT.md
- The join screen handles invalid, expired, and used codes inline without navigation loss

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
