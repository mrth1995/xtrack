---
phase: 04-pwa-realtime
plan: 06
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/lib/pwa/installability.ts
  - src/lib/pwa/offline-shell.ts
  - src/lib/install/visibility.ts
  - src/lib/components/InstallGuidanceBanner.svelte
  - src/routes/(app)/+layout.svelte
  - src/routes/(app)/+page.svelte
autonomous: true
requirements:
  - PWA-01
  - PWA-03
  - PWA-04
must_haves:
  truths:
    - "Authenticated browser users see platform-aware install guidance; standalone users do not."
    - "After a prior visit, the app shell loads offline and shows a compact non-blocking offline/stale notice."
  artifacts:
    - path: "src/lib/pwa/installability.ts"
      provides: "Platform-aware install guidance state"
      exports: ["getInstallGuidanceState", "shouldShowInstallGuidance"]
    - path: "src/lib/pwa/offline-shell.ts"
      provides: "Browser online/offline shell state helpers"
      exports: ["createOfflineShellState"]
    - path: "src/lib/components/InstallGuidanceBanner.svelte"
      provides: "iOS/Android install guidance UI"
      contains: "Install app"
    - path: "src/routes/(app)/+page.svelte"
      provides: "Offline/stale notice placement in the signed-in Quick Add shell"
      contains: "createOfflineShellState"
  key_links:
    - from: "src/lib/components/InstallGuidanceBanner.svelte"
      to: "src/lib/pwa/installability.ts"
      via: "platform-aware banner state"
      pattern: "getInstallGuidanceState"
    - from: "src/routes/(app)/+page.svelte"
      to: "src/lib/pwa/offline-shell.ts"
      via: "offline notice state"
      pattern: "createOfflineShellState"
---

<objective>
Wire the interactive PWA install guidance and offline shell notice on top of the static PWA foundation from `04-02`.

Purpose: PWA-01, PWA-03, and PWA-04 require browser-specific install guidance and visible offline/stale shell behavior after service-worker metadata is in place.
Output: Platform-aware installability helpers, upgraded install banner, offline shell state, and signed-in shell notice placement.
</objective>

<execution_context>
@/Users/ridwan.taufik/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ridwan.taufik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-pwa-realtime/04-CONTEXT.md
@.planning/phases/04-pwa-realtime/04-RESEARCH.md
@.planning/phases/04-pwa-realtime/04-PATTERNS.md
@.planning/phases/04-pwa-realtime/04-UI-SPEC.md
@.planning/phases/04-pwa-realtime/04-VALIDATION.md
@.planning/phases/04-pwa-realtime/04-01-SUMMARY.md
@.planning/phases/04-pwa-realtime/04-02-SUMMARY.md
@src/lib/install/visibility.ts
@src/lib/components/InstallGuidanceBanner.svelte
@src/routes/(app)/+layout.svelte
@src/routes/(app)/+page.svelte
@tests/pwa/installability.test.ts
@tests/e2e/pwa-offline-shell.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade platform-aware install guidance</name>
  <files>src/lib/pwa/installability.ts, src/lib/install/visibility.ts, src/lib/components/InstallGuidanceBanner.svelte</files>
  <read_first>
    - src/lib/install/visibility.ts
    - src/lib/components/InstallGuidanceBanner.svelte
    - tests/pwa/installability.test.ts
    - .planning/phases/04-pwa-realtime/04-CONTEXT.md
    - .planning/phases/04-pwa-realtime/04-UI-SPEC.md
    - .planning/phases/04-pwa-realtime/04-PATTERNS.md
  </read_first>
  <behavior>
    - D-03/PWA-01: iOS Safari gets manual instruction copy.
    - D-03/PWA-01: Android/Chromium with `beforeinstallprompt` gets `Install app` CTA.
    - D-01/PWA-01: unsupported browsers get `Open this page in Safari or Chrome to add it to your Home Screen.`
    - D-03: standalone mode and snoozed state hide the banner.
  </behavior>
  <action>Create `src/lib/pwa/installability.ts` with exported types `InstallPlatform = 'ios' | 'android' | 'unsupported'`, `InstallGuidanceState`, and functions `isStandalone()`, `detectInstallPlatform(userAgent: string)`, `getInstallGuidanceState(isAuthenticated: boolean, deferredPromptAvailable?: boolean)`, `shouldShowInstallGuidance(isAuthenticated: boolean, deferredPromptAvailable?: boolean)`, `snoozeInstallGuidance(durationMs?: number)`, and `clearInstallGuidanceSnooze()`. Keep the snooze key `xtrack_install_snoozed_until` and default 24 hours. Update `src/lib/install/visibility.ts` to re-export from `$lib/pwa/installability` so older imports keep working. Update `InstallGuidanceBanner.svelte` to listen for `beforeinstallprompt`, store the event in component state, render the exact UI-SPEC copy per platform, show a 48px high accent-filled button with text `Install app` only when the deferred prompt exists, call `prompt()` on click, and keep the dismiss button `aria-label="Dismiss install guidance"` with a minimum 44px touch target. Do not show the banner in standalone mode.</action>
  <verify>
    <automated>npm run test:unit -- tests/pwa/installability.test.ts</automated>
  </verify>
  <done>Installability tests pass and the component renders iOS, Android, unsupported, snoozed, and standalone states.</done>
  <acceptance_criteria>
    - `rg -n "export type InstallPlatform|beforeinstallprompt|xtrack_install_snoozed_until|getInstallGuidanceState|shouldShowInstallGuidance|Open this page in Safari or Chrome" src/lib/pwa/installability.ts src/lib/install/visibility.ts src/lib/components/InstallGuidanceBanner.svelte`
    - `rg -n "Add to Home Screen|Tap Share, then Add to Home Screen\\.|Install xtrack|Add xtrack to your Home Screen for faster expense logging\\.|Install app|Dismiss install guidance" src/lib/components/InstallGuidanceBanner.svelte`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add offline shell state and non-blocking notice</name>
  <files>src/lib/pwa/offline-shell.ts, src/routes/(app)/+layout.svelte, src/routes/(app)/+page.svelte</files>
  <read_first>
    - src/routes/(app)/+layout.svelte
    - src/routes/(app)/+page.svelte
    - src/lib/offline/expense-sync.ts
    - tests/e2e/pwa-offline-shell.spec.ts
    - .planning/phases/04-pwa-realtime/04-CONTEXT.md
    - .planning/phases/04-pwa-realtime/04-UI-SPEC.md
    - .planning/phases/04-pwa-realtime/04-PATTERNS.md
  </read_first>
  <behavior>
    - D-02/PWA-03: offline app shell displays before stale/offline notice.
    - UI-SPEC: offline notice copy is exactly `You're offline. We'll show saved data and sync when you're back online.`
    - UI-SPEC: stale notice copy is exactly `Showing saved data. New expenses may appear when you're back online.`
    - Notice is non-modal and does not cover the numpad, categories, or list.
  </behavior>
  <action>Create `src/lib/pwa/offline-shell.ts` exporting `createOfflineShellState()` that returns Svelte-compatible state or an object with `online`, `stale`, `message`, `markStale()`, and `destroy()`; it must read `navigator.onLine`, listen to `online` and `offline`, and use the exact offline/stale copy from the UI-SPEC. Wire it into `src/routes/(app)/+page.svelte` directly below `<InstallGuidanceBanner />` and above the GearMenu area. Render a compact `<div role="status">` only when offline or stale. Use `background: var(--color-surface)`, text `var(--color-muted)`, `rounded-xl`, `p-4`, and no fixed/absolute overlay. In `src/routes/(app)/+layout.svelte`, when standalone session restoration fails due to offline state, keep the existing `Restoring your session...` gate from becoming a browser error and allow the cached shell to show the offline notice where app data exists. Do not add modal, toast, snackbar, or bottom sheet behavior.</action>
  <verify>
    <automated>npm run check && npx playwright test tests/e2e/pwa-offline-shell.spec.ts --project=chromium</automated>
  </verify>
  <done>Offline shell Playwright test passes and the signed-in app displays the compact notice while offline.</done>
  <acceptance_criteria>
    - `test -f src/lib/pwa/offline-shell.ts`
    - `rg -n "createOfflineShellState|navigator\\.onLine|addEventListener\\('offline'|addEventListener\\('online'|You're offline\\. We'll show saved data and sync when you're back online\\.|Showing saved data\\. New expenses may appear when you're back online\\." src/lib/pwa/offline-shell.ts src/routes/(app)/+page.svelte`
    - `rg -n "role=\"status\"|rounded-xl|p-4|InstallGuidanceBanner" src/routes/(app)/+page.svelte`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user agent to install guidance | User agent and install prompt state control instructional UI only. |
| offline shell to authenticated content | Cached app shell must not leak private API responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Spoofing | `InstallGuidanceBanner.svelte` | accept | Banner does not authenticate or collect secrets; it only displays browser install instructions. |
| T-04-09 | Information Disclosure | standalone restore/offline notice | mitigate | Offline notice copy contains no user data and the preceding PWA config must not include private API cache rules. |
| T-04-10 | Denial of Service | offline shell state listeners | mitigate | Return cleanup/destroy behavior for browser event listeners. |
</threat_model>

<verification>
Run `npm run test:unit -- tests/pwa/installability.test.ts`, `npm run check`, and `npx playwright test tests/e2e/pwa-offline-shell.spec.ts --project=chromium`.
</verification>

<success_criteria>
PWA-01 and PWA-03 interactive checks pass; install guidance follows iOS/Android platform behavior; offline shell notice follows UI-SPEC copy and placement.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-realtime/04-06-SUMMARY.md`.
</output>
