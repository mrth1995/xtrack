---
phase: 04-pwa-realtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/pwa/installability.test.ts
  - tests/pwa/manifest.test.ts
  - tests/e2e/pwa-offline-shell.spec.ts
  - tests/realtime/expense-merge.test.ts
  - tests/realtime/expense-subscription.test.ts
autonomous: true
requirements:
  - PWA-01
  - PWA-03
  - PWA-04
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "Installability, manifest, offline shell, realtime merge, and realtime subscription behaviors have executable RED tests before implementation."
    - "Validation files from 04-VALIDATION.md exist with concrete assertions for every Phase 04 requirement."
  artifacts:
    - path: "tests/pwa/installability.test.ts"
      provides: "Install guidance and standalone visibility tests"
      contains: "shouldShowInstallGuidance"
    - path: "tests/pwa/manifest.test.ts"
      provides: "PWA manifest metadata tests"
      contains: "display: standalone"
    - path: "tests/e2e/pwa-offline-shell.spec.ts"
      provides: "Offline app-shell browser acceptance test"
      contains: "serviceWorker.ready"
    - path: "tests/realtime/expense-merge.test.ts"
      provides: "client_id skip, queued replacement, and partner highlight tests"
      contains: "mergeRealtimeExpense"
    - path: "tests/realtime/expense-subscription.test.ts"
      provides: "Supabase channel filter and cleanup tests"
      contains: "household_id=eq."
  key_links:
    - from: "tests/realtime/expense-subscription.test.ts"
      to: "src/lib/realtime/expenses.ts"
      via: "future import"
      pattern: "subscribeToExpenseInserts"
    - from: "tests/realtime/expense-merge.test.ts"
      to: "src/lib/realtime/expense-merge.ts"
      via: "future import"
      pattern: "mergeRealtimeExpense"
---

<objective>
Create the Phase 04 Wave 0 validation scaffold.

Purpose: The implementation plans must have Nyquist coverage for PWA installability/offline shell behavior and realtime household merge behavior before production code changes.
Output: Five test files named in `.planning/phases/04-pwa-realtime/04-VALIDATION.md`.
</objective>

<execution_context>
@/Users/ridwan.taufik/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ridwan.taufik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-pwa-realtime/04-CONTEXT.md
@.planning/phases/04-pwa-realtime/04-RESEARCH.md
@.planning/phases/04-pwa-realtime/04-PATTERNS.md
@.planning/phases/04-pwa-realtime/04-UI-SPEC.md
@.planning/phases/04-pwa-realtime/04-VALIDATION.md
@tests/setup.ts
@playwright.config.ts
@tests/offline/expense-sync.test.ts
@tests/e2e/offline-quick-add.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PWA installability and manifest RED tests</name>
  <files>tests/pwa/installability.test.ts, tests/pwa/manifest.test.ts</files>
  <read_first>
    - tests/setup.ts
    - src/lib/install/visibility.ts
    - src/lib/components/InstallGuidanceBanner.svelte
    - src/app.html
    - vite.config.ts
    - package.json
    - .planning/phases/04-pwa-realtime/04-CONTEXT.md
    - .planning/phases/04-pwa-realtime/04-UI-SPEC.md
    - .planning/phases/04-pwa-realtime/04-VALIDATION.md
  </read_first>
  <behavior>
    - PWA-01/PWA-04: tests assert iOS Safari authenticated non-standalone shows heading `Add to Home Screen` and body `Tap Share, then Add to Home Screen.`
    - PWA-01: tests assert Android/Chromium install prompt state shows heading `Install xtrack`, body `Add xtrack to your Home Screen for faster expense logging.`, and CTA `Install app`.
    - PWA-01: tests assert standalone mode hides install guidance.
    - PWA-04: tests assert manifest fields `name: xtrack`, `short_name: xtrack`, `display: standalone`, `theme_color: #176B5D`, `start_url: /`, and icon references for 192, 512, and maskable 512 assets.
  </behavior>
  <action>Create the two Vitest files. Use dynamic imports through string constants for future modules that do not exist yet: `$lib/pwa/installability` and `../../static/manifest.webmanifest?raw` or an equivalent filesystem read helper. Do not add implementation stubs. Tests may be RED because `@vite-pwa/sveltekit`, manifest files, or `$lib/pwa/installability` do not exist yet. Include exact expected strings from the UI-SPEC: `Add to Home Screen`, `Tap Share, then Add to Home Screen.`, `Install xtrack`, `Add xtrack to your Home Screen for faster expense logging.`, `Install app`, `Open this page in Safari or Chrome to add it to your Home Screen.`</action>
  <verify>
    <automated>npm run test:unit -- tests/pwa/installability.test.ts tests/pwa/manifest.test.ts</automated>
  </verify>
  <done>Vitest collects both files and fails only because future PWA implementation modules/assets are missing or assertions are not yet satisfied.</done>
  <acceptance_criteria>
    - `test -f tests/pwa/installability.test.ts`
    - `test -f tests/pwa/manifest.test.ts`
    - `rg -n "Add to Home Screen|Tap Share, then Add to Home Screen\\.|Install xtrack|Install app|shouldShowInstallGuidance|standalone" tests/pwa/installability.test.ts`
    - `rg -n "name.*xtrack|short_name.*xtrack|display.*standalone|theme_color.*#176B5D|192|512|maskable" tests/pwa/manifest.test.ts`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add offline app-shell Playwright RED test</name>
  <files>tests/e2e/pwa-offline-shell.spec.ts</files>
  <read_first>
    - tests/e2e/offline-quick-add.spec.ts
    - playwright.config.ts
    - src/hooks.server.ts
    - src/routes/(app)/+page.server.ts
    - src/routes/(app)/+layout.svelte
    - .planning/phases/04-pwa-realtime/04-VALIDATION.md
    - .planning/phases/04-pwa-realtime/04-UI-SPEC.md
  </read_first>
  <behavior>
    - PWA-03: first online visit reaches the authenticated Quick Add shell under `PLAYWRIGHT_E2E_FIXTURE=1`.
    - PWA-03: test waits for `navigator.serviceWorker.ready`.
    - PWA-03/D-02: after `context.setOffline(true)` and `page.reload()`, the app shell still shows `Amount` and the offline notice copy `You're offline. We'll show saved data and sync when you're back online.`
  </behavior>
  <action>Create a Playwright spec that uses the existing localhost fixture style from `tests/e2e/offline-quick-add.spec.ts`. The spec must navigate to `/`, wait for the amount display, wait for `navigator.serviceWorker.ready`, set the browser context offline, reload, and assert the app shell still renders instead of a browser error page. Do not make the test depend on remote Supabase credentials.</action>
  <verify>
    <automated>npx playwright test tests/e2e/pwa-offline-shell.spec.ts --project=chromium --list</automated>
  </verify>
  <done>Playwright lists the offline shell test. Running it may stay RED until service worker/offline shell implementation lands.</done>
  <acceptance_criteria>
    - `test -f tests/e2e/pwa-offline-shell.spec.ts`
    - `rg -n "serviceWorker\\.ready|setOffline\\(true\\)|You're offline\\. We'll show saved data and sync when you're back online\\.|Amount" tests/e2e/pwa-offline-shell.spec.ts`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add realtime merge and subscription RED tests</name>
  <files>tests/realtime/expense-merge.test.ts, tests/realtime/expense-subscription.test.ts</files>
  <read_first>
    - src/routes/(app)/+page.svelte
    - src/lib/components/ExpenseList.svelte
    - src/lib/offline/types.ts
    - src/lib/supabase/client.ts
    - tests/offline/expense-sync.test.ts
    - .planning/phases/04-pwa-realtime/04-CONTEXT.md
    - .planning/phases/04-pwa-realtime/04-RESEARCH.md
    - .planning/phases/04-pwa-realtime/04-PATTERNS.md
    - .planning/phases/04-pwa-realtime/04-VALIDATION.md
  </read_first>
  <behavior>
    - SYNC-02/D-07: `mergeRealtimeExpense` returns the original array reference and `highlightedClientId: null` when incoming `client_id` already exists locally on a synced row.
    - SYNC-01/D-08: an incoming server row with a `client_id` matching a local `sync_status: queued` or `syncing` row replaces that row, clears sync status, and preserves its index.
    - SYNC-01/D-05: a new partner row is inserted in `spent_at` descending order and returns `highlightedClientId` equal to the incoming `client_id`.
    - SYNC-01: `subscribeToExpenseInserts` calls Supabase `.on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'expenses', filter: 'household_id=eq.${householdId}' }, callback)` and cleanup calls `supabase.removeChannel(channel)`.
  </behavior>
  <action>Create the realtime test files using dynamic imports through constants for `$lib/realtime/expense-merge` and `$lib/realtime/expenses`. Mock the Supabase client object in the subscription test with `channel`, `on`, `subscribe`, and `removeChannel` spies. Do not add production implementation stubs. Use exact field names `id`, `client_id`, `household_id`, `amount`, `category`, `note`, `spent_at`, `sync_status`, and `server_id`.</action>
  <verify>
    <automated>npm run test:unit -- tests/realtime/expense-merge.test.ts tests/realtime/expense-subscription.test.ts</automated>
  </verify>
  <done>Vitest collects both realtime test files and fails only because future realtime modules or assertions are not yet satisfied.</done>
  <acceptance_criteria>
    - `test -f tests/realtime/expense-merge.test.ts`
    - `test -f tests/realtime/expense-subscription.test.ts`
    - `rg -n "mergeRealtimeExpense|highlightedClientId|queued|syncing|client_id|preserve|spent_at" tests/realtime/expense-merge.test.ts`
    - `rg -n "subscribeToExpenseInserts|postgres_changes|household_id=eq\\.\\$\\{householdId\\}|removeChannel|INSERT|public|expenses" tests/realtime/expense-subscription.test.ts`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture to browser | Localhost-only Playwright fixture simulates authenticated app state. |
| mocked realtime payload to merge tests | Test data represents untrusted rows that production code must validate before UI merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | PWA offline shell tests | mitigate | Assert tests target shell/static behavior and do not require caching private API response bodies. |
| T-04-02 | Information Disclosure | realtime subscription tests | mitigate | Assert the filter string is exactly `household_id=eq.${householdId}`. |
| T-04-03 | Tampering | realtime merge tests | mitigate | Assert `client_id` duplicate/queued replacement behavior before implementation. |
| T-04-04 | Spoofing | install guidance tests | accept | Install guidance is instructional UI only and does not authenticate users. |
</threat_model>

<verification>
Run `npm run test:unit -- tests/pwa/installability.test.ts tests/pwa/manifest.test.ts tests/realtime/expense-merge.test.ts tests/realtime/expense-subscription.test.ts` and `npx playwright test tests/e2e/pwa-offline-shell.spec.ts --project=chromium --list`.
</verification>

<success_criteria>
All five Wave 0 validation files exist, collect under Vitest/Playwright, and encode PWA-01, PWA-03, PWA-04, SYNC-01, and SYNC-02.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-realtime/04-01-SUMMARY.md`.
</output>
