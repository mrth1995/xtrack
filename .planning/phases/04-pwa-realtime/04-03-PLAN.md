---
phase: 04-pwa-realtime
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql
  - .planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md
autonomous: false
requirements:
  - SYNC-01
must_haves:
  truths:
    - "The `public.expenses` table is enabled for Supabase Realtime/Postgres Changes before live sync verification."
    - "Existing RLS remains the household access boundary and the client subscription will additionally filter by `household_id`."
  artifacts:
    - path: "supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql"
      provides: "Idempotent publication migration for public.expenses"
      contains: "supabase_realtime"
    - path: ".planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md"
      provides: "Schema push/verification evidence"
      contains: "supabase db push"
  key_links:
    - from: "supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql"
      to: "Supabase Realtime"
      via: "publication membership"
      pattern: "ALTER PUBLICATION supabase_realtime ADD TABLE public.expenses"
---

<objective>
Add and apply the required Supabase Realtime publication configuration for `public.expenses`.

Purpose: Supabase app checks can pass while live Postgres Changes are disabled; this blocking plan prevents false-positive Phase 04 verification.
Output: A migration and schema-push evidence showing `public.expenses` is in the `supabase_realtime` publication while existing RLS policies remain intact.
</objective>

<execution_context>
@/Users/ridwan.taufik/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ridwan.taufik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-pwa-realtime/04-CONTEXT.md
@.planning/phases/04-pwa-realtime/04-RESEARCH.md
@.planning/phases/04-pwa-realtime/04-PATTERNS.md
@.planning/phases/04-pwa-realtime/04-VALIDATION.md
@supabase/config.toml
@supabase/migrations/2026042501_phase1_foundation.sql
@supabase/migrations/2026042702_fix_rls_policy_inlining.sql
@supabase/migrations/2026050201_phase3_idempotent_expense_insert.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent expenses Realtime publication migration</name>
  <files>supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql</files>
  <read_first>
    - supabase/migrations/2026042501_phase1_foundation.sql
    - supabase/migrations/2026042702_fix_rls_policy_inlining.sql
    - supabase/migrations/2026050201_phase3_idempotent_expense_insert.sql
    - supabase/config.toml
    - .planning/phases/04-pwa-realtime/04-RESEARCH.md
    - .planning/phases/04-pwa-realtime/04-CONTEXT.md
  </read_first>
  <action>Create `supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql`. The migration must not disable or alter RLS. It must include an idempotent `DO $$ ... $$;` block that checks `pg_publication_tables` for `pubname = 'supabase_realtime'`, `schemaname = 'public'`, and `tablename = 'expenses'`; when absent, execute `ALTER PUBLICATION supabase_realtime ADD TABLE public.expenses`. Add a verification comment with the SQL query `SELECT pubname, schemaname, tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime' AND schemaname = 'public' AND tablename = 'expenses';`. Do not add `service_role` keys or client-side secrets.</action>
  <verify>
    <automated>rg -n "pg_publication_tables|supabase_realtime|ALTER PUBLICATION supabase_realtime ADD TABLE public\\.expenses|tablename = 'expenses'|schemaname = 'public'" supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql</automated>
  </verify>
  <done>Migration exists and is idempotent by checking publication membership before adding `public.expenses`.</done>
  <acceptance_criteria>
    - `test -f supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql`
    - `rg -n "DO \\$\\$|pg_publication_tables|pubname = 'supabase_realtime'|tablename = 'expenses'|ALTER PUBLICATION supabase_realtime ADD TABLE public\\.expenses" supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql`
    - `! rg -n "disable row level security|service_role|anonKey|PUBLIC_SUPABASE" supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push or manually verify Realtime publication</name>
  <files>.planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md</files>
  <read_first>
    - supabase/migrations/2026050301_phase4_expenses_realtime_publication.sql
    - .planning/phases/04-pwa-realtime/04-RESEARCH.md
    - .planning/phases/04-pwa-realtime/04-VALIDATION.md
  </read_first>
  <what-built>The Phase 04 migration that adds `public.expenses` to the `supabase_realtime` publication.</what-built>
  <how-to-verify>
    1. First try automated CLI push: run `supabase db push`.
    2. If non-interactive CLI auth is unavailable, set `SUPABASE_ACCESS_TOKEN` and rerun `supabase db push`.
    3. If CLI push cannot be completed locally, open Supabase Dashboard SQL editor for the project and run the migration SQL manually.
    4. Verify with SQL: `SELECT pubname, schemaname, tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime' AND schemaname = 'public' AND tablename = 'expenses';`
    5. Expected result: one row with `supabase_realtime | public | expenses`.
  </how-to-verify>
  <action>Create `.planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md` recording the exact command path used (`supabase db push`, `SUPABASE_ACCESS_TOKEN` non-TTY push, or Dashboard SQL), the verification SQL above, and the observed result `supabase_realtime | public | expenses`. If the user performs the dashboard step, record `User confirmed remote Realtime publication for public.expenses on YYYY-MM-DD`. This task is blocking; do not proceed to final Phase 04 UAT until this evidence file exists.</action>
  <verify>
    <automated>test -f .planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md && rg -n "supabase db push|SUPABASE_ACCESS_TOKEN|Dashboard SQL|pg_publication_tables|supabase_realtime \\| public \\| expenses|User confirmed remote Realtime publication" .planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md</automated>
  </verify>
  <resume-signal>Type "approved" after the migration is pushed or the dashboard SQL verification returns `supabase_realtime | public | expenses`; otherwise describe the credential or SQL error.</resume-signal>
  <done>Remote Supabase project has `public.expenses` in `supabase_realtime`, and evidence is recorded.</done>
  <acceptance_criteria>
    - `test -f .planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md`
    - `rg -n "supabase_realtime|public \\| expenses|pg_publication_tables|supabase db push|Dashboard SQL|User confirmed" .planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database publication to Realtime clients | Postgres row changes become WebSocket payloads for authorized clients. |
| local CLI to remote Supabase project | Schema push modifies remote database publication state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | Supabase Realtime publication | mitigate | Publish only `public.expenses`; rely on existing `expenses_household_select` RLS and require client-side `household_id` filter in later plan. |
| T-04-10 | Elevation of Privilege | schema push process | mitigate | Do not use or store `service_role` in repo; use Supabase CLI auth or dashboard-only SQL. |
| T-04-11 | Tampering | migration idempotency | mitigate | Check `pg_publication_tables` before executing `ALTER PUBLICATION`. |
| T-04-12 | Repudiation | remote schema state | mitigate | Record push/verification evidence in `04-REALTIME-SCHEMA.md`. |
</threat_model>

<verification>
Run the migration grep command, complete the blocking schema push or dashboard verification, and confirm `.planning/phases/04-pwa-realtime/04-REALTIME-SCHEMA.md` contains the verification SQL result.
</verification>

<success_criteria>
`public.expenses` is enabled for Supabase Realtime/Postgres Changes before final live household sync verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-realtime/04-03-SUMMARY.md`.
</output>
