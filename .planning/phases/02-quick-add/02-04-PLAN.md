---
phase: 02-quick-add
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
  - 02-03
files_modified: []
autonomous: false
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04
  - INPUT-05
  - INPUT-06
  - INPUT-07
  - INPUT-12
  - INPUT-13
  - INPUT-14
tags:
  - manual-verification
  - human-uat
  - phase-gate

must_haves:
  truths:
    - "User opens app at / after login and lands on the Quick Add numpad — no dashboard, no menu (INPUT-01, D-01)"
    - "Tapping 5,4,0,0,0 on the numpad displays '54.000' in real time (INPUT-02, INPUT-03)"
    - "All 7 category tiles render in 3-3-1 layout with the correct emojis from D-13 (INPUT-04, D-13)"
    - "Tapping a category with an entered amount saves the expense (it appears in the today list) (INPUT-05)"
    - "Double-tapping a category does NOT create a duplicate row in the today list (INPUT-06, D-14)"
    - "Bottom sheet slides up after save with the saved expense summary; Skip and Save note dismiss (INPUT-14, D-07/D-08)"
    - "Tapping a today-list row navigates to /expenses/{id}/edit (D-06)"
    - "Editing amount, category, note, date and saving updates the row (INPUT-12, D-17)"
    - "Two-tap delete soft-deletes the row; it disappears from / and /expenses (INPUT-13)"
    - "Numpad never opens the device system keyboard (D-05) — verified on iOS Safari"
    - "WIB date boundary: an expense logged at 06:00 WIB shows in 'today' (not yesterday) (Pitfall 6)"
    - "Server-side errors (including 500 from duplicate client_id UNIQUE constraint) surface as error toast — confirmed via code inspection and test with invalid input (Codex HIGH: duplicate-save idempotency gap)"
    - "RLS policies expenses_creator_update and expenses_household_select are confirmed live in Supabase (Codex HIGH: edit/delete route authorization)"
  artifacts: []
  key_links: []
---

<objective>
Phase 2 acceptance gate. Confirm via direct device interaction that the user-facing behaviours specified by ROADMAP.md Phase 2 success criteria are met. Automated tests pin the contract; this checkpoint pins the experience.

Purpose: Many Phase 2 requirements (INPUT-01 numpad-first navigation, INPUT-02 system-keyboard suppression, INPUT-06 visual debounce feedback, INPUT-14 sheet animation) are not fully testable in jsdom — they require a real browser or device. This is the explicit human verification step before declaring Phase 2 complete.

Output: A signed-off Phase 2 — every must-have truth confirmed or escalated to a gap-closure plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-quick-add/02-CONTEXT.md
@.planning/phases/02-quick-add/02-UI-SPEC.md
@.planning/phases/02-quick-add/02-VALIDATION.md
@.planning/phases/02-quick-add/02-01-PLAN.md
@.planning/phases/02-quick-add/02-02-PLAN.md
@.planning/phases/02-quick-add/02-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — green test suite + clean svelte-check + dev server boot</name>
  <files></files>
  <read_first>
    - .planning/phases/02-quick-add/02-VALIDATION.md (sampling rate + manual-only verifications table)
    - .planning/phases/02-quick-add/02-02-PLAN.md (composition of the home screen — for what to verify)
    - .planning/phases/02-quick-add/02-03-PLAN.md (composition of /expenses + edit — for what to verify)
  </read_first>
  <action>
Before handing off to the human verifier, automate the gate:

1. Run the full Phase 2 test suite:
   `npm run test -- tests/expenses/ --reporter=verbose 2>&1 | tail -60`
   ALL four test files (formatters, numpad, quick-add, edit) MUST be green.

2. Run svelte-check:
   `npm run check 2>&1 | tail -15`
   Zero errors, zero warnings beyond the baseline.

3. Run the full unit suite to detect any cross-phase regression:
   `npm run test 2>&1 | tail -20`
   No previously passing tests went red.

4. Boot the dev server in the background (the orchestrator may already have done this):
   `npm run dev &` and confirm the server prints `Local:   http://localhost:5173/` (or similar). Note the URL for the human checkpoint.

If any of steps 1-3 fail, STOP. Do not advance to the human checkpoint. Capture the failing output and either flag a gap-closure plan via /gsd-plan-phase --gaps or ask the user how to proceed.
  </action>
  <verify>
    <automated>npm run test -- tests/expenses/ --reporter=verbose 2>&1 | tail -20 && npm run check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- tests/expenses/` exits 0.
    - `npm run check` exits 0.
    - `npm run test` exits 0 (no cross-phase regression).
    - Dev server is reachable at the printed URL (verified by the orchestrator before handing off).
  </acceptance_criteria>
  <done>
    Automated gates pass; the app is running and ready for human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 2 acceptance — manual UAT walk-through</name>
  <what-built>
    Phase 2 has shipped:
    - P01 (Wave 0): formatters + Zod schemas + ExpenseList shared component.
    - P02 (Wave 1): Quick Add home page (numpad + categories + today list + note sheet + gear menu) — replaces the Phase 1 household overview.
    - P03 (Wave 1): Full history (/expenses) + edit/delete (/expenses/[id]/edit) with WIB date round-trip and two-step delete.

    Automated tests cover IDR formatting, schema validation, server-action contracts, and the Numpad component shell. The remaining behaviours below require a real browser (ideally iOS Safari or Chromium DevTools mobile emulation) to verify.
  </what-built>
  <how-to-verify>
**Setup:** Open http://localhost:5173/ in Chrome (mobile emulation) AND on an iPhone (Safari, if available). Log in with your test household account.

---

**Block A — Quick Add core (INPUT-01, INPUT-02, INPUT-03, INPUT-04, INPUT-05)**

1. After login the URL should be `/` and the screen should show: install banner (or hidden if standalone), gear icon top-right, big "0" amount display, 12-key numpad, 7 category tiles in 3-3-1 layout, "Today" header, and "View all history →" link.
   - **Pass criterion:** No dashboard, no menu, no extra screen between login and the numpad (INPUT-01).
2. Tap 5, 4, 0, 0, 0 on the numpad. The amount display should update in real time:
   `5 → 54 → 540 → 5.400 → 54.000`.
   - **Pass criterion:** dot separator, no currency symbol, instant feedback (INPUT-02, INPUT-03).
3. Confirm the 7 category tiles render with these exact emojis (D-13):
   Row 1: 🍜 Food, 🚗 Transport, 🎬 Entertainment.
   Row 2: 💡 Utilities, 🛍️ Shopping, ❤️ Health.
   Row 3: ✅ Others.
4. With "54.000" entered, tap Food. The note bottom sheet should slide up showing "🍜 Food · Rp 54.000". The amount display should reset to "0".
   - **Pass criterion:** save fires, numpad resets, sheet slides up (INPUT-05, D-04, D-07).

---

**Block B — Debounce + system-keyboard suppression (INPUT-06, D-05)**

5. Dismiss the note sheet via "Skip". Tap the numpad to enter another amount, then double-tap rapidly on "Transport". Only ONE Transport row should appear in the today list (after the sheet dismisses).
   - **Pass criterion:** 500ms debounce prevents duplicate (INPUT-06).
6. On iOS Safari (or mobile emulation), tap any numpad digit. The device system keyboard MUST NOT open.
   - **Pass criterion:** D-05 — buttons-only numpad, no `<input type="number">` anywhere.

---

**Block C — Note sheet + history (INPUT-14, D-08, D-09, D-16)**

7. Enter another amount, tap a category. In the bottom sheet, type "lunch" and tap "Save note". The sheet should close and the today-list row should now show "lunch" next to the category.
8. Save another expense, this time tap "Skip" instead of "Save note". The sheet closes; the row appears without a note. Reload the page — Skip should NOT re-prompt (D-09).
9. Tap "View all history →" at the bottom. URL navigates to /expenses (D-16).

---

**Block D — Edit + delete (INPUT-12, INPUT-13, D-06, D-17)**

10. On /expenses, expenses should be grouped under a date label (e.g. "26 Apr"). Tap any row.
    - **Pass criterion:** URL is `/expenses/{id}/edit` (D-06).
11. The edit form should show 4 editable fields: amount, category (3-3-1 grid with current category highlighted), note, date.
12. Change the amount to a new value (e.g. clear and type "75000"), pick a different category, edit/clear the note, change the date back one day. Tap "Save changes".
    - **Pass criterion:** redirect to /expenses; the row reflects the new values; date label moved to the previous day (INPUT-12, D-17).
13. Open the same expense again. Tap "Delete expense". Button text changes to "Tap again to confirm delete" with destructive outline. Tap again.
    - **Pass criterion:** redirect to /; the row is gone from both / today list AND /expenses history (INPUT-13 — soft delete).
14. (Optional but recommended) Open Supabase SQL editor and run `SELECT id, is_deleted FROM expenses WHERE id = '<deleted-id>'`. The row should still exist with `is_deleted = true` (NOT a hard delete).
    - **Codex HIGH — saveNote privilege escalation verification:** In the Supabase SQL editor, run `SELECT id, household_id, created_by FROM expenses ORDER BY created_at DESC LIMIT 5`. Note the `id` of an expense from your household. Then confirm that the `saveNote` action on any `expense_id` you do NOT own (from a different `created_by`) silently returns success but does NOT update the note — the `expenses_creator_update` RLS policy blocks it. (This requires a second test account; if unavailable, document as "deferred to Phase 3 cross-account test".)
    - **Codex HIGH — edit/delete RLS regression check:** Run `SELECT policyname FROM pg_policies WHERE tablename = 'expenses'` in the Supabase SQL editor. Confirm the output includes `expenses_creator_update` and `expenses_household_select`. If either is missing, the authorization boundary for saveEdit and deleteExpense is broken and must be fixed before Phase 2 is marked complete.

---

**Block E — Gear menu (D-15)**

15. Tap the ⚙ gear icon top-right. Dropdown shows: Invite member, Household details, Log out.
16. Tap "Invite member" → /settings/invite loads. Tap browser back to /. Tap gear → "Household details" → /household loads.
17. Tap gear → "Log out". You're signed out and redirected to /auth.

---

**Block F — Edge cases (Pitfall coverage)**

18. WIB boundary: Set your device clock to 06:00 WIB (or just verify with whatever time it is). Log a fresh expense at the current time. It MUST appear in the "Today" list immediately.
    - **Pass criterion:** WIB date window is correct (Pitfall 6).
19. Amount overflow: Try to enter "999.999.9990" (i.e. type 9 ten times). The 10th digit should be ignored.
    - **Pass criterion:** max 99.999.999 enforced (UI-SPEC interaction step 3).
20. Empty-amount tap: With amount display showing "0", tap a category. Nothing should happen — no save, no sheet.
    - **Pass criterion:** category tap is a no-op when amount is empty.
21. **Codex HIGH — duplicate-save idempotency error toast:** Log an expense normally. Then, to simulate a duplicate `client_id` collision, reload the page (which resets state) but do NOT log a new expense — instead, note that in production a network retry that reuses the same `client_id` would hit the UNIQUE constraint and return a 500. Verify via code inspection that the `use:enhance` failure branch in `+page.svelte` calls `showError()` for ALL non-success results (not just specific status codes). Check `grep -c 'showError' src/routes/(app)/+page.svelte` returns at least 2. Verify from a real 400/500 response (e.g., temporarily submit amount=0) that the error toast appears inline and auto-clears after ~4 seconds.
    - **Pass criterion:** Any server-side failure (including 500 from duplicate client_id) surfaces as an error toast visible to the user, is never silently swallowed (Codex HIGH: duplicate-save idempotency gap). Phase 3 will replace the 500 with idempotent `ON CONFLICT DO NOTHING`.
  </how-to-verify>
  <resume-signal>
Type "approved" if every block passed (including steps 21, and the Codex HIGH security checks in step 14). If any block failed, list which sub-step(s) failed and what you observed (e.g. "Block B step 6: system keyboard opened on iPhone Safari", or "Step 14 RLS check: expenses_creator_update policy not found"). Failures will trigger a gap-closure plan via `/gsd-plan-phase 02 --gaps`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verifier → live app | Subjective verification; verifier must follow the script faithfully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Verification gap | manual UAT script | accept | Human verification is the only sufficient check for INPUT-01 navigation, INPUT-06 visual debounce timing, D-05 system-keyboard suppression, and INPUT-14 CSS animation. The 20-step script above covers every behaviour the automated suite cannot. If the human verifier discovers a regression, gap-closure flow takes over. |
| T-02-04-02 | Verification gap | post-launch security check | mitigate | Block D step 14 instructs the verifier to confirm via Supabase SQL editor that delete is soft (`is_deleted = true`), not physical — the most consequential security guarantee for Phase 2 (INPUT-13 + T-02 mitigation). |
| T-02-04-03 | Tampering | dev server bound to all interfaces | accept | Dev server runs on localhost only (`npm run dev` default). No network exposure during UAT. |
</threat_model>

<verification>
Phase 2 is complete when:
1. The pre-flight automated gate (Task 1) is green.
2. Every block in Task 2's verification script passed (verifier typed "approved").
3. ROADMAP.md Phase 2 success criteria 1-5 each map to one or more passed verification steps:
   - SC1 (numpad first screen) ↔ Block A step 1.
   - SC2 (digits → IDR display + tile saves) ↔ Block A steps 2, 4.
   - SC3 (no double-save) ↔ Block B step 5.
   - SC4 (edit) ↔ Block D steps 11, 12.
   - SC5 (delete soft) ↔ Block D steps 13, 14.
4. **Codex HIGH security gates:**
   - Duplicate-save idempotency error toast: step 21 verified (error surfaces as toast, not silently swallowed).
   - saveNote privilege escalation: step 14 Codex HIGH note verified (RLS blocks cross-user note update; OR documented as deferred to Phase 3 cross-account test).
   - Edit/delete RLS regression: step 14 Codex HIGH regression check verified (`expenses_creator_update` and `expenses_household_select` confirmed live in Supabase `pg_policies`).
</verification>

<success_criteria>
- All automated tests for Phase 2 are green.
- All 20 manual verification steps in Task 2 pass.
- Verifier signs off with "approved".
- Any failures spawn a gap-closure plan; Phase 2 is NOT marked complete with open gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quick-add/02-04-SUMMARY.md` documenting:
- The verifier's approval (or list of failures and the gap-closure plan number that addresses them).
- Device + browser used for verification (Chrome desktop, Chrome mobile emulation, iPhone Safari, etc).
- Any non-blocking observations to file as Phase 3+ candidates (e.g. "smoother sheet animation would be nice" — but never silently demote Phase 2 scope).
- The screenshot(s) the verifier captured (optional but encouraged, especially for the WIB-boundary verification).

After this plan, mark all 10 Phase 2 requirements as Complete in REQUIREMENTS.md and ROADMAP.md.
</output>
