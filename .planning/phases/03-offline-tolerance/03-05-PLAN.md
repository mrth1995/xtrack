---
phase: 03-offline-tolerance
plan: 05
type: execute
wave: 3
depends_on:
  - 03-04
files_modified:
  - tests/e2e/offline-quick-add.spec.ts
  - .planning/phases/03-offline-tolerance/03-UAT.md
autonomous: true
requirements:
  - INPUT-08
  - INPUT-09
  - INPUT-10
  - INPUT-11
must_haves:
  truths:
    - "The browser offline flow passes: offline save -> Waiting row -> online flush -> status disappears."
    - "Retrying a queued expense does not create duplicate server rows."
    - "The full phase gate runs check, unit tests, and Playwright offline flow."
  artifacts:
    - path: "tests/e2e/offline-quick-add.spec.ts"
      provides: "Executable browser acceptance flow"
      contains: ["setOffline(true)", "Waiting", "toHaveCount(1)"]
    - path: ".planning/phases/03-offline-tolerance/03-UAT.md"
      provides: "Phase 03 automated and browser verification evidence"
      contains: ["INPUT-08", "INPUT-09", "INPUT-10", "INPUT-11"]
  key_links:
    - from: "tests/e2e/offline-quick-add.spec.ts"
      to: "src/routes/(app)/+page.svelte"
      via: "user-facing Quick Add interactions"
      pattern: "getByRole\\('button'"
---

<objective>
Run and harden the final automated Phase 03 acceptance gate.

Purpose: Phase 03 requires unit/integration coverage plus browser-level offline proof before verification can pass.
Output: Passing Playwright offline flow and UAT evidence file with exact command results.
</objective>

<execution_context>
@/Users/ridwan.taufik/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ridwan.taufik/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-offline-tolerance/03-CONTEXT.md
@.planning/phases/03-offline-tolerance/03-VALIDATION.md
@.planning/phases/03-offline-tolerance/03-01-SUMMARY.md
@.planning/phases/03-offline-tolerance/03-02-SUMMARY.md
@.planning/phases/03-offline-tolerance/03-03-SUMMARY.md
@.planning/phases/03-offline-tolerance/03-04-SUMMARY.md
@tests/e2e/offline-quick-add.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the Playwright offline flow executable against the implemented app</name>
  <files>tests/e2e/offline-quick-add.spec.ts</files>
  <read_first>tests/e2e/offline-quick-add.spec.ts, playwright.config.ts, src/routes/(app)/+page.svelte, src/lib/components/ExpenseList.svelte, .planning/phases/03-offline-tolerance/03-CONTEXT.md</read_first>
  <action>Remove any Wave 0 fixture TODOs that are no longer needed. Ensure the test reaches authenticated Quick Add using the project's existing auth strategy or a deterministic test fixture. Keep the exact D-26 assertions: offline save, visible `Waiting`, online dispatch, status disappears, and one `Food` row remains. If Supabase test data is required, seed only test-scoped data and do not weaken RLS or use service-role credentials in browser code. Run a fast Playwright listing smoke check before the full browser run so syntax/fixture discovery fails quickly; the full Playwright execution remains intentional here because this plan is the final browser acceptance gate required by D-24 through D-26.</action>
  <verify>
    <automated>npx playwright test tests/e2e/offline-quick-add.spec.ts --list && npx playwright test tests/e2e/offline-quick-add.spec.ts</automated>
  </verify>
  <acceptance_criteria>`tests/e2e/offline-quick-add.spec.ts` contains no `Requires authenticated test fixture before execution` comment; it contains `setOffline(true)`, `getByText('Waiting')`, `setOffline(false)`, `new Event('online')`, and `toHaveCount(1)`; `npx playwright test tests/e2e/offline-quick-add.spec.ts --list` lists the test before the full browser run; the verify command exits 0.</acceptance_criteria>
  <done>Browser-level offline acceptance test passes against the implemented app.</done>
</task>

<task type="auto">
  <name>Task 2: Run phase gate and record verification evidence</name>
  <files>.planning/phases/03-offline-tolerance/03-UAT.md</files>
  <read_first>.planning/phases/03-offline-tolerance/03-VALIDATION.md, .planning/phases/03-offline-tolerance/03-CONTEXT.md, .planning/phases/03-offline-tolerance/03-02-SUMMARY.md, tests/e2e/offline-quick-add.spec.ts</read_first>
  <action>Run a fast browser-test discovery smoke first: `npx playwright test tests/e2e/offline-quick-add.spec.ts --list`. Then run the full Phase 03 gate exactly: `npm run check && npm run test && npx playwright test tests/e2e/offline-quick-add.spec.ts`. The full Playwright run is intentional in this task because it is the final Phase 03 gate required by D-24 through D-26. Create `.planning/phases/03-offline-tolerance/03-UAT.md` with sections `Commands Run`, `Requirement Evidence`, and `Security Evidence`. Under Requirement Evidence, include one bullet each for `INPUT-08`, `INPUT-09`, `INPUT-10`, and `INPUT-11` with the test or source artifact that proves it. Under Security Evidence, include one bullet each for IndexedDB sensitive data, household mismatch, replay/idempotency, expired auth, RLS denial retry loops, and migration/RPC access control.</action>
  <verify>
    <automated>npx playwright test tests/e2e/offline-quick-add.spec.ts --list && npm run check && npm run test && npx playwright test tests/e2e/offline-quick-add.spec.ts</automated>
  </verify>
  <acceptance_criteria>`.planning/phases/03-offline-tolerance/03-UAT.md` contains `Commands Run`, `Requirement Evidence`, `Security Evidence`, `INPUT-08`, `INPUT-09`, `INPUT-10`, `INPUT-11`, `IndexedDB sensitive data`, `household mismatch`, `replay/idempotency`, `expired auth`, `RLS denial retry loops`, and `migration/RPC access control`; `npx playwright test tests/e2e/offline-quick-add.spec.ts --list` lists the test before the full gate; the verify command exits 0.</acceptance_criteria>
  <done>Phase 03 has recorded automated evidence for all requirements and required threat mitigations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser test -> app UI | Test exercises the user-visible offline workflow. |
| app UI -> IndexedDB -> server | Acceptance verifies data moves from local queue to Supabase once online. |
| verification evidence -> release decision | UAT file must not claim success without command evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | IndexedDB sensitive data | mitigate | UAT evidence must point to queue tests/source showing no auth tokens or service-role keys are stored. |
| T-03-02 | Tampering/Elevation | Tampered queued payload household mismatch | mitigate | UAT evidence must point to sync classification/source that marks mismatched household rows failed. |
| T-03-03 | Tampering/Repudiation | Replay/idempotency | mitigate | Playwright asserts one Food row after offline retry; UAT references RPC `ON CONFLICT (client_id) DO NOTHING`. |
| T-03-04 | Spoofing | Expired auth | mitigate | UAT evidence must point to session-ready pause/auth failure classification. |
| T-03-05 | Denial of Service | RLS denial retry loops | mitigate | UAT evidence must point to failed classification/no hot-loop test. |
| T-03-06 | Elevation | Database migration/RPC access control | mitigate | UAT evidence must point to schema push summary and migration strings for `REVOKE`, `GRANT EXECUTE`, and membership checks. |
</threat_model>

<verification>
Final gate: `npm run check && npm run test && npx playwright test tests/e2e/offline-quick-add.spec.ts`.
</verification>

<success_criteria>
The final gate exits 0 and UAT evidence maps every Phase 03 requirement plus every required threat area to a concrete command or file artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/03-offline-tolerance/03-05-SUMMARY.md`.
</output>
