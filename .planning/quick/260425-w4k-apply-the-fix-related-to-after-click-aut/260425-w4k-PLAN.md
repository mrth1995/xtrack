# Quick Task 260425-w4k Plan

**Date:** 2026-04-25
**Description:** apply the fix related to after click auth in email, it'll redirect to localhost:5173 with access token but it only show login form

## Goal

Make Supabase email confirmation links establish a usable session in the app so the user does not land back on a plain login form after clicking the email link.

## Tasks

### 1. Fix SSR auth persistence
- Update the browser and server Supabase clients to use SSR-compatible cookie persistence.
- Ensure server-side auth reads restored sessions correctly in `hooks.server.ts`.

### 2. Complete the email confirmation flow
- Keep signup confirmation links pointing back to this app.
- Consume auth redirect fragments on `/auth`, show a transient confirmation state, and navigate authenticated users into the app.

### 3. Verify the fix
- Run Svelte type checks.
- Run the existing auth/session unit tests.

